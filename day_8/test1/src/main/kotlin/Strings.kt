
var str : String = "test"

var upper = str.toUpperCase()
var lower = str.toLowerCase()
val sub = str.subSequence(0, 4)

val assertStr = str.contains("te")

val containsStrSpecifyCase = str.contains("Te", ignoreCase = false)
